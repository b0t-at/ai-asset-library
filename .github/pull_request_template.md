## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] Token changes are in `tokens/*.json`; `npm run build` was run and generated files are committed (`npm run check` passes)
- [ ] Contrast table printed by the build has no `FAIL`
- [ ] New/changed components are documented in `docs/components.md` and rendered in `examples/showcase.html`
- [ ] Checked in light **and** dark, at 360px and 1280px, keyboard only
- [ ] Logo changes: SVG and `scripts/build-brand.mjs` geometry updated together, `npm run build:brand` run
- [ ] `CHANGELOG.md` entry added and `package.json` version bumped if consumers can see the change
